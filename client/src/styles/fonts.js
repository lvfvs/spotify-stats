import { css } from "styled-components/macro";

const fonts = css`
  @font-face {
    font-family: "circular";
    src: url("../assetsfonts/circular-book.otf") format("otf");
    font-weight: 400;
    font-style: normal;
  }
`;

export default fonts;
